def solution(date1, date2):
    date1tostr = ''
    date2tostr = ''
    
    for i, j in zip(date1, date2):
        date1tostr += str(i)
        date2tostr += str(j)
        
    if (int(date1tostr) >= int(date2tostr)):
        return 0
    else:
        return 1
